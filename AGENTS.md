# AGENTS.md

# Backend Interview Bible 프로젝트 운영 규칙

> 이 문서는 Backend Interview Bible 프로젝트의 운영 규칙을 정의합니다.
>
> Codex 또는 AI는 문서를 생성하기 전에 반드시 이 문서를 먼저 읽고 따라야 합니다.

---

# 1. 프로젝트 목적

Backend Interview Bible은 Java 백엔드 개발자를 위한 실무 중심 기술 핸드북 프로젝트입니다.

이 프로젝트의 목표는 다음과 같습니다.

- Java 백엔드 핵심 기술 정리
- Spring 생태계 정리
- 운영 환경(Production) 경험 정리
- 시스템 설계(System Design) 학습
- 기술 면접 대비
- 지속적으로 성장 가능한 기술 문서 구축

문서의 개수를 늘리는 것이 목적이 아닙니다.

모든 문서는 프로젝트 전체의 품질을 높이는 방향으로 작성되어야 합니다.

---

# 2. AI의 역할

AI는 단순히 문서를 생성하는 도구가 아닙니다.

다음 역할을 수행해야 합니다.

- 새로운 문서 작성
- 기존 문서와 중복 여부 확인
- 문서 간 연결 유지
- 프로젝트 전체의 일관성 유지
- 인덱스 최신화
- 품질 기준 준수

---

# 3. 핵심 원칙

## 3.1 정확성을 최우선으로 한다.

잘못된 정보를 작성하지 않는다.

확실하지 않은 내용은 추측하지 않는다.

---

## 3.2 실무 중심으로 작성한다.

단순한 정의만 작성하지 않는다.

다음을 함께 설명한다.

- 왜 사용하는가
- 언제 사용하는가
- 언제 사용하면 안 되는가
- 실무에서는 어떻게 사용하는가

---

## 3.3 운영 환경을 고려한다.

가능한 경우 다음 내용을 포함한다.

- 운영 중 발생 가능한 문제
- 장애 사례
- 성능 이슈
- 메모리 이슈
- 병목 현상
- 운영 경험

---

## 3.4 면접을 고려한다.

모든 문서는 기술 면접 준비에 도움이 되어야 한다.

단순한 사용법보다

"왜 그렇게 동작하는가"

를 설명한다.

---

## 3.5 장기 유지보수를 고려한다.

현재 문서 하나만 생각하지 않는다.

300개 이상의 문서가 생성되어도 유지될 수 있도록 작성한다.

---

# 4. 품질 기준

프로젝트의 기준 문서는

**001. Java Architecture**

이다.

새로운 문서는 최소한 다음 수준 이상의 품질을 유지해야 한다.

- 기술적 깊이
- 설명의 명확성
- 실무 활용성
- 운영 환경 고려
- 성능 분석
- 기술 면접 활용도

---

# 5. 기술 기준

기본 기술 기준은 다음과 같다.

| 항목 | 기준 |
|------|------|
| Java | 17 LTS |
| Spring Framework | 6.x |
| Spring Boot | 3.x |
| Hibernate | 6.x |
| Gradle | Kotlin DSL 권장 |

Java 21 이상의 기능은 별도 설명으로 추가할 수 있다.

---

# 6. 문서 생성 원칙

문서를 작성할 때는 다음 순서를 따른다.

1. 기존 문서 확인
2. 중복 여부 확인
3. 문서 생성
4. 자가 검토
5. 인덱스 갱신

---

# 7. 문서 중복 금지

이미 존재하는 내용을 다시 작성하지 않는다.

주제가 겹치는 경우에는

- 기존 문서를 보강하거나
- 관련 문서를 링크한다.

---

# 8. 저장소 구조 준수

문서는 반드시 지정된 폴더에 생성한다.

임의의 폴더를 생성하지 않는다.

문서의 위치는 프로젝트 구조를 따른다.

## 8.1 사용자 지정 경로 강제

사용자가 문서 생성 경로를 명시한 경우 해당 경로를 최우선으로 따른다.

사용자의 사전 승인을 받지 않고 다음 항목을 다른 경로에 생성하지 않는다.

- 설계 문서
- 구현 계획 문서
- 작업 메모
- 임시 문서
- 보조 산출물

프로젝트 규칙상 인덱스처럼 반드시 수정해야 하는 기존 파일은 갱신할 수 있지만,
새로운 파일이나 폴더는 사용자가 지정한 경로 밖에 생성하지 않는다.

도구 또는 작업 절차가 별도 경로의 문서 생성을 권장하더라도 이 규칙을 우선한다.

---

# 9. 인덱스 관리

문서를 생성하거나 수정한 경우 반드시 다음 파일을 함께 갱신한다.

- PROBLEM-INDEX.md
- HANDBOOK-INDEX.md

두 파일은 항상 최신 상태를 유지해야 한다.

---

# 10. 기존 문서 존중

기존 문서를 임의로 삭제하거나 대규모 수정하지 않는다.

기존 내용을 변경해야 하는 경우에는

- 기존 내용을 유지하면서 개선하거나
- 필요한 내용을 추가한다.

---

# 11. 금지 사항

다음 행위는 금지한다.

- 중복 문서 생성
- 사실이 아닌 내용 작성
- 근거 없는 추측
- 프로젝트 구조 변경
- 기존 문서 삭제
- 인덱스 미갱신
- 품질 기준 이하의 문서 작성

---

# 12. 작업 완료 기준

다음 조건을 모두 만족해야 작업이 완료된 것으로 본다.

- 문서 생성 완료
- 문서 검토 완료
- 인덱스 갱신 완료
- 프로젝트 규칙 준수
- 품질 기준 충족

---

# 13. 최종 목표

이 프로젝트의 목표는

**Java 백엔드 개발자를 위한 실무 중심 기술 핸드북을 구축하는 것**이다.

모든 문서는 다음 기준을 만족해야 한다.

- 읽기 쉬운 문서
- 실무에서 도움이 되는 문서
- 기술 면접에 도움이 되는 문서
- 장기간 유지보수가 가능한 문서
- 프로젝트 전체와 일관성을 유지하는 문서
---

# 14. Codex 단축 명령 및 번호 기반 워크플로우

Codex는 프로젝트 루트를 작업 디렉터리로 연 상태에서 다음 단축 명령을 인식해야 한다.

## 14.1 세 자리 번호 명령

사용자 입력 전체가 다음 형식 중 하나와 정확히 일치하면 `PROBLEM-INDEX.md`의 해당 번호 문서 생성 요청으로 해석한다.

```text
NNN
NNN번 생성
```

예:

```text
004
004번 생성
```

- `NNN`은 `001`부터 `350`까지의 세 자리 번호다.
- 세 자리 숫자를 임의의 주제명이나 일반 대화로 해석하지 않는다.
- 범위 밖 번호, 존재하지 않는 번호 또는 형식이 불명확한 입력은 파일을 만들지 말고 문제를 보고한다.
- 번호의 주제, 상태와 난이도는 반드시 `PROBLEM-INDEX.md`에서 읽는다.

번호 명령을 받으면 다음 순서로 작업한다.

1. `AGENTS.md`, `README.md`, `PROBLEM-INDEX.md`와 `HANDBOOK-INDEX.md`를 읽는다.
2. 요청 번호의 정확한 주제와 현재 상태를 확인한다.
3. 기존 볼륨, Markdown 장과 관련 주제를 확인한다.
4. 동일 번호, 동일 파일명과 유사·중복 문서를 검색한다.
5. 실제 프로젝트 구조와 주제의 주된 목적에 따라 기존 볼륨 중 하나를 선택한다.
6. 생성 예정 절대경로를 작업 출력으로 먼저 알린다.
7. 상태가 `⬜`이고 기존 파일과 충돌하지 않을 때만 장 하나를 생성한다.
8. Java 17과 관련된 경우 Spring Boot 3.x를 기본으로 한국어 UTF-8 Markdown을 작성한다.
9. 기술 내용, 코드, Markdown, Mermaid 및 Wiki Link를 자체 검증한다.
10. 검증 성공 후 요청 번호 상태만 `⬜`에서 `🟨`로 변경한다.
11. `HANDBOOK-INDEX.md`의 올바른 볼륨과 `Recently Added`에 중복 없이 추가한다.
12. 다른 번호, 다른 장, `README.md`와 `ROADMAP.md`는 수정하지 않는다.
13. 생성 파일, 변경 파일, 상태 변경, 연결 문서, 검증 결과와 불확실성을 보고한다.

다음 상황에서는 새 문서를 생성하거나 기존 문서를 덮어쓰지 않는다.

- 번호 상태가 이미 `🟨`, `🟩` 또는 `🔁`인 경우
- 동일 번호 또는 동일 파일명이 이미 존재하는 경우
- 유사 주제 문서와 실질적으로 중복되는 경우
- `PROBLEM-INDEX.md` 상태와 실제 파일 존재 여부가 불일치하는 경우
- 대상 볼륨을 합리적으로 결정할 수 없는 경우

이 경우 현재 상태와 기존 파일 경로 또는 분류 충돌을 보고하고 사용자 지시를 기다린다.

## 14.2 `next` 명령

사용자 입력 전체가 `next`와 정확히 일치하면 다음 절차를 수행한다.

1. `PROBLEM-INDEX.md`의 유효한 문제 행 전체를 확인한다.
2. 상태가 `⬜`인 항목 중 번호가 가장 빠른 하나를 선택한다.
3. 선택한 번호와 주제를 보고한다.
4. 해당 번호에 대해 14.1의 번호 기반 워크플로우를 그대로 수행한다.
5. 한 번의 요청에서 다음 번호까지 연속 생성하지 않는다.

유효한 미작성 항목이 없으면 주제를 추측하거나 새 항목을 만들지 말고 완료 상태를 보고한다.

## 14.3 `status` 명령

사용자 입력 전체가 `status`와 정확히 일치하면 읽기 전용으로 다음을 확인한다.

- 전체 문제 수
- `⬜`, `🟨`, `🟩`, `🔁` 상태별 개수
- 현재 초안과 수정 필요 항목
- 실제 생성된 장 파일 수와 경로
- 다음 미작성 번호와 주제
- `PROBLEM-INDEX.md`, `HANDBOOK-INDEX.md`와 실제 파일의 불일치

`status` 처리 중에는 어떤 파일도 생성하거나 수정하지 않는다.

## 14.4 상태 전이

상태는 다음 기준으로 변경한다.

```text
⬜ → 문서 생성 및 자체 검증 완료 → 🟨
🟨 → 외부 기술 리뷰, 수정 및 최종 검증 완료 → 🟩
검토 중 수정 필요 확인 → 🔁
```

- 문서를 만들었다는 이유만으로 바로 `🟩`로 변경하지 않는다.
- 생성 또는 검증에 실패하면 상태를 변경하지 않는다.
- 최종 완료 요청에서는 파일, 번호, 주제, 폴더, 필수 섹션, 코드, Mermaid, Wiki Link와 인덱스를 모두 검증한 후 해당 번호만 `🟨 → 🟩`로 변경한다.
- 다른 번호의 상태, 순서와 주제를 변경하지 않는다.

## 14.5 세션 시작 원칙

새 PC 또는 새 Codex 세션에서는 프로젝트 루트를 작업 디렉터리로 열어야 한다.

Codex가 프로젝트 루트의 `AGENTS.md`를 읽은 뒤에는 세 자리 번호, `next`, `status` 단축 명령을 이 섹션에 따라 처리한다. 초기 Clone 후에는 [[CODEX-PROMPT-GUIDE#1 최초 실행 프롬프트|최초 실행 프롬프트]]로 파일 및 상태 충돌을 한 번 확인하는 것을 권장한다.

