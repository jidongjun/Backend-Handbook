# Backend Interview Bible Codex 프롬프트 가이드

> [!info]
> 이 문서는 다른 PC나 새로운 Codex 세션에서도 동일한 번호 기반 문서 생성 절차를 재현하기 위한 프롬프트 모음이다. 프로젝트 운영 규칙은 항상 [[AGENTS]]가 최우선이며, 문제와 상태의 기준은 [[PROBLEM-INDEX]]다.

## 공통 원칙

- 현재 디렉터리를 프로젝트 루트로 연 뒤 사용한다.
- 프로젝트 루트 밖의 파일을 읽거나 수정하지 않는다.
- 한 번에 핸드북 장 하나만 생성한다.
- 기존 장을 덮어쓰지 않는다.
- 문서는 Java 17과 Spring Boot 3.x를 기본으로 하는 한국어 UTF-8 Markdown으로 작성한다.
- 실제로 존재하는 관련 문서에만 Obsidian Wiki Link를 추가한다.
- 생성 성공 시 해당 문제만 `⬜ → 🟨`로 변경한다.
- 기술 리뷰와 최종 검증이 끝난 경우에만 `🟨 → 🟩`로 변경한다.

## 1. 최초 실행 프롬프트

새 PC에서 저장소를 Clone했거나 새 Codex 세션을 시작했을 때 한 번 사용한다.

```text
현재 디렉터리는 Backend Interview Bible 프로젝트 루트다.

어떤 파일도 생성하거나 수정하기 전에 다음 문서를 읽어라.

- AGENTS.md
- README.md
- PROBLEM-INDEX.md
- HANDBOOK-INDEX.md
- ROADMAP.md

이후 모든 작업에서는 AGENTS.md를 최우선 규칙으로 적용해라.

반드시 다음 원칙을 지켜라.

1. 프로젝트 루트 밖의 파일은 읽거나 수정하지 않는다.
2. 기존 문서를 임의로 삭제하거나 덮어쓰지 않는다.
3. PROBLEM-INDEX.md의 번호, 주제와 상태를 기준으로 작업한다.
4. 한 번의 생성 요청에서는 주제 문서 하나만 만든다.
5. 문서는 한국어 UTF-8 Markdown으로 작성하고 일반적인 기술 용어는 영어를 유지할 수 있다.
6. Java 17과 Spring Boot 3.x를 기본 기준으로 사용한다.
7. 관련 문서가 실제로 존재할 때만 Obsidian Wiki Link를 추가한다.
8. 생성 전에 동일 번호, 동일 파일명과 유사 주제 문서를 검색한다.
9. 파일이 이미 존재하면 덮어쓰지 말고 작업을 중단한 뒤 경로와 상태 불일치를 보고한다.
10. 생성과 검증에 성공한 경우 해당 번호 상태만 ⬜에서 🟨로 변경한다.
11. HANDBOOK-INDEX.md의 올바른 볼륨과 Recently Added를 갱신한다.
12. 다른 문제의 상태, 순서와 주제를 수정하지 않는다.
13. README.md와 ROADMAP.md는 명시적인 요청이나 구조 변경이 없으면 수정하지 않는다.

지금은 장을 생성하지 마라.

프로젝트 구조와 문서를 확인한 뒤 다음 내용만 보고하라.

- 이해한 작업 방식
- 실제 볼륨과 번호별 저장 폴더 규칙
- 전체 문제 수와 상태별 개수
- 기존 장 목록
- 다음 미작성 번호
- 발견한 파일 또는 상태 충돌
- 작업 시작 가능 여부
```

## 2. 번호 지정 생성 프롬프트

아래 `031`을 원하는 세 자리 번호로 바꾼다.

```text
PROBLEM-INDEX.md의 031번 주제 문서 하나만 생성해라.

작업 전에 AGENTS.md, README.md, PROBLEM-INDEX.md, HANDBOOK-INDEX.md를 다시 확인해라.

작업 순서:

1. 031번의 상태, 주제, 난이도와 분류를 확인한다.
2. 실제 프로젝트 구조와 AGENTS.md에 따라 저장 폴더와 파일명을 결정한다.
3. 동일 번호, 동일 파일명과 유사 주제 문서를 검색한다.
4. 파일이 이미 존재하면 덮어쓰지 말고 작업을 중단한 뒤 기존 파일 경로를 보고한다.
5. PROBLEM-INDEX.md 상태와 실제 파일이 불일치해도 새 파일을 만들지 말고 충돌을 보고한다.
6. 문제가 없으면 031번 문서 하나만 생성한다.
7. Java 17을 기준으로 설명과 실행 가능한 예제를 작성한다.
8. Spring이 직접 관련된 경우에만 Spring Boot 3.x 예제를 포함한다.
9. 개념, 배경, 내부 동작, 트레이드오프, 성능, 운영 사례와 면접 내용을 자연스럽게 연결한다.
9-1. `## 면접 답변 압축` 섹션을 반드시 추가하고, 1~2분 안에 말할 수 있는 5~8문장 답변으로 작성한다.
10. 유용한 경우 Obsidian 호환 Mermaid를 포함한다.
11. 실제로 존재하는 문서에만 Obsidian Wiki Link를 추가한다.
12. 기술적 정확성, 코드, Markdown, Mermaid와 Wiki Link를 자체 검증한다.
13. 생성과 검증에 성공하면 031번 상태만 ⬜에서 🟨로 변경한다.
14. HANDBOOK-INDEX.md의 올바른 볼륨과 Recently Added에 문서를 추가한다.
15. 다른 번호, 장, README.md와 ROADMAP.md는 수정하지 않는다.

완료 후 다음 내용을 보고하라.

- 생성한 문서 경로
- 수정한 파일
- 문서 번호와 주제
- 상태 변경 결과
- 연결한 관련 문서
- 자체 검증 결과
- 불확실하거나 추가 검토가 필요한 내용
```

설정 확인 후에는 다음처럼 짧게 요청할 수 있다.

```text
031번 생성
```

## 3. 다음 미작성 문서 생성 프롬프트

```text
AGENTS.md, README.md, PROBLEM-INDEX.md와 HANDBOOK-INDEX.md를 읽어라.

PROBLEM-INDEX.md에서 상태가 ⬜인 항목 중 번호가 가장 빠른 주제 하나를 선택해 해당 문서만 생성해라.

1. PROBLEM-INDEX.md 전체에서 유효한 문제 행만 확인한다.
2. 상태가 ⬜인 가장 빠른 번호 하나를 선택한다.
3. 선택 번호, 주제와 현재 상태를 보고한다.
4. 실제 프로젝트 구조와 AGENTS.md에 따라 저장 위치를 결정한다.
5. 동일 번호, 동일 파일명과 유사 문서가 존재하는지 확인한다.
6. 파일과 상태가 불일치하면 생성하지 말고 충돌을 보고한다.
7. 문제가 없으면 선택한 문서 하나만 생성한다.
8. Java 17 및 관련된 경우 Spring Boot 3.x 기준을 적용한다.
8-1. `## 면접 답변 압축` 섹션을 반드시 추가하고, 정의·원리·실무 중요성·주의점·마무리 문장 흐름으로 작성한다.
9. 기술적 정확성, 코드, Markdown, Mermaid와 Wiki Link를 자체 검증한다.
10. 성공한 경우 선택 번호 상태만 ⬜에서 🟨로 변경한다.
11. HANDBOOK-INDEX.md의 올바른 볼륨과 Recently Added를 갱신한다.
12. 다음 번호 문서를 생성하지 않는다.
13. 다른 문제 상태, 장, README.md와 ROADMAP.md를 수정하지 않는다.

완료 후 선택 번호, 주제, 생성 경로, 수정 파일, 상태 변경과 검증 결과를 보고하라.
```

간단 명령:

```text
next
```

## 4. 상태 확인 프롬프트

```text
PROBLEM-INDEX.md와 실제 장 파일을 읽기 전용으로 확인해라.

다음 내용만 보고하고 파일은 수정하지 마라.

- 전체 문제 수
- ⬜, 🟨, 🟩, 🔁 상태별 개수
- 현재 초안 및 수정 필요 항목
- 실제 생성된 장 수
- 다음 미작성 번호와 주제
- 인덱스와 실제 파일의 불일치
```

간단 명령:

```text
status
```

## 5. 문서 자체 검토 프롬프트

번호와 경로를 명시해 사용한다.

```text
방금 생성한 031번 문서를 다시 검토하고 발견한 문제를 해당 문서에 직접 수정해라.

검토 기준:

- 기술적 오류와 검증되지 않은 단정
- Java 17 기준의 정확성
- Spring 관련 내용이 있다면 Spring Boot 3.x 기준의 정확성
- 내부 동작 설명의 깊이
- 코드의 문법, 실행 가능성과 본문 일치 여부
- 잘못된 성능 또는 메모리 설명
- 현실성이 부족하거나 사실처럼 표현된 가상 운영 사례
- 면접 질문, 후속 질문, `면접 답변 압축`과 Senior-Level Answer의 품질
- Mermaid 문법
- 존재하지 않는 Obsidian Wiki Link
- 중복되거나 불필요하게 긴 설명

기존의 좋은 내용과 구조는 유지한다.
다른 장과 PROBLEM-INDEX.md의 다른 번호는 수정하지 않는다.
현재 번호의 상태는 🟨로 유지한다.
필요한 경우 HANDBOOK-INDEX.md의 해당 링크 설명만 일치하도록 수정한다.

완료 후 수정 내역, 검증 결과와 아직 확실하지 않은 부분을 보고하라.
```

## 6. 외부 리뷰 반영 프롬프트

```text
아래는 현재 문서에 대한 외부 기술 리뷰다.

[여기에 리뷰 내용을 붙여 넣는다]

리뷰를 현재 문서에 반영해라.

1. 리뷰를 무조건 적용하지 말고 기술적으로 타당한지 검증한다.
2. 명백한 기술 오류를 가장 먼저 수정한다.
3. 기존 문서의 좋은 설명과 구조를 보존한다.
4. 부족한 내부 동작, 운영 사례, 성능과 장애 대응 관점을 보완한다.
5. 코드와 본문의 설명을 일치시킨다.
6. Java 17과 관련된 경우 Spring Boot 3.x 기준을 유지한다.
7. 중복 설명을 정리한다.
8. Mermaid와 실제 Wiki Link를 다시 검증한다.
9. 다른 장과 다른 문제 상태는 수정하지 않는다.
10. 최종 완료 검증 전까지 현재 상태는 🟨로 유지한다.

완료 후 반영한 내용, 제외한 리뷰와 그 이유, 남은 불확실성을 보고하라.
```

## 7. 최종 완료 상태 변경 프롬프트

```text
현재 문서의 작성과 기술 리뷰가 완료되었다.

다음을 읽기 전용으로 먼저 검증해라.

1. 문서 파일이 실제로 존재하고 비어 있지 않은가?
2. 파일 번호와 주제가 PROBLEM-INDEX.md와 일치하는가?
3. 올바른 폴더와 파일명인가?
4. 필수 문서 섹션과 H1이 유효한가? 특히 `## 면접 답변 압축` 섹션이 존재하고 독립 답변으로 읽히는가?
5. 코드 블록과 Mermaid 문법에 문제가 없는가?
6. 모든 Wiki Link가 실제 문서를 가리키는가?
7. HANDBOOK-INDEX.md에 중복 없이 등록되어 있는가?
8. 미해결 기술 오류나 검증되지 않은 버전별 단정이 없는가?

모든 검증을 통과한 경우에만 PROBLEM-INDEX.md에서 해당 번호 상태를 🟨에서 🟩로 변경해라.
해당 번호 이외의 상태, 문서 본문과 다른 파일은 수정하지 않는다.
검증에 실패하면 상태를 변경하지 말고 실패 항목을 보고하라.
```

## 8. 다른 PC에서 사용하는 순서

1. GitHub 저장소를 GitHub Desktop으로 Clone한다.
2. Clone된 폴더를 Obsidian에서 **Open folder as vault**로 연다.
3. Codex에서 같은 폴더를 작업 디렉터리로 연다.
4. 이 문서의 [[CODEX-PROMPT-GUIDE#1 최초 실행 프롬프트|최초 실행 프롬프트]]를 한 번 실행한다.
5. 매 작업 전 Pull하고, 작업 후 변경을 검토한 뒤 Commit과 Push를 수행한다.

```text
매일 시작: Pull → status
문서 생성: next 또는 NNN번 생성
생성 직후: 문서 자체 검토
외부 리뷰 후: 외부 리뷰 반영
최종 검증 후: 🟨 → 🟩
작업 종료: Commit → Push
```

